#include<stdio.h>
int main()
{char ch;
scanf("%c,&ch");
int a;
if(ch=='A',ch=='z');
printf("the given character isin upper case");
 if(ch>='a'&& ch<='z')
printf(" the given character  is in lower case");
else if(ch>='0' && ch<='9')
printf(" the given character  is in digit");
else
printf(" the given character is an symbol");
return 0;
}